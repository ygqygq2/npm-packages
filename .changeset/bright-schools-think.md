---
"@ygqygq2/eslint-config": patch
---

Add a `taro-react` flat config based on the official Taro 4 React template rules, expose it through package exports, and upgrade the package to the ESLint 10 compatible dependency set.
